# مخططات UML — بوابة Golden Atlas الفندقية
## مشروع نهاية الدراسة — PFE

---

## 1. مخطط حالات الاستخدام

```mermaid
flowchart TD
    زائر(["👤 زائر غير مسجل"])
    عميل(["👤 عميل مسجل"])
    مدير(["🛡️ مدير النظام"])
    ذكاء(["🤖 مساعد Atlas Chat\nDeepSeek API"])
    قاعدة(["🗄️ قاعدة البيانات\nSupabase"])

    UC1["تصفح محتوى الموقع"]
    UC2["تغيير لغة الواجهة\nعربي / فرنسي / إنجليزي / إسباني"]
    UC3["إنشاء حساب جديد"]
    UC4["تسجيل الدخول\nبريد إلكتروني أو Google"]
    UC5["حجز غرفة أو خدمة"]
    UC6["عرض حجوزاتي"]
    UC7["إلغاء حجز"]
    UC8["استخدام نقاط الولاء"]
    UC9["التحدث مع مساعد Atlas"]
    UC10["تصفح القوائم والمعارض"]
    UC11["إدارة جميع الحجوزات"]
    UC12["إدارة المستخدمين"]
    UC13["إدارة محتوى الموقع"]

    زائر --> UC1
    زائر --> UC2
    زائر --> UC3
    زائر --> UC4

    عميل --> UC1
    عميل --> UC2
    عميل --> UC5
    عميل --> UC6
    عميل --> UC7
    عميل --> UC8
    عميل --> UC9
    عميل --> UC10

    مدير --> UC11
    مدير --> UC12
    مدير --> UC13

    UC5 -.->|"«يشمل»"| قاعدة
    UC6 -.->|"«يشمل»"| قاعدة
    UC9 -.->|"«يشمل»"| ذكاء
    UC4 -.->|"«يمتد»"| UC5
    UC8 -.->|"«يمتد»"| UC5

    style زائر fill:#1a1a2e,color:#e0c97f,stroke:#e0c97f,stroke-width:2px
    style عميل fill:#16213e,color:#e0c97f,stroke:#e0c97f,stroke-width:2px
    style مدير fill:#0f3460,color:#e0c97f,stroke:#e0c97f,stroke-width:2px
    style ذكاء fill:#533483,color:#fff,stroke:#a78bfa,stroke-width:2px
    style قاعدة fill:#1a3c2e,color:#4ade80,stroke:#4ade80,stroke-width:2px
```

**الوصف:** يُظهر هذا المخطط الأطراف الثلاثة الرئيسية في النظام (زائر، عميل مسجل، مدير) إضافةً إلى الطرفين الثانويين (الذكاء الاصطناعي وقاعدة البيانات). يوضح 13 حالة استخدام مع علاقات الشمول والامتداد بينها.

---

## 2. مخطط الفئات

```mermaid
classDiagram
    direction TB

    class مستخدم {
        +String المعرف
        +String الاسم
        +String البريدالإلكتروني
        +String كلمةالمرور
        +String الدور
        +Date تاريخالتسجيل
        +تسجيلالدخول()
        +تسجيلالخروج()
        +تعديلالملفالشخصي()
    }

    class عميل {
        +int نقاطالولاء
        +List الحجوزات
        +إجراءحجز(خدمة)
        +إلغاءحجز(المعرف)
        +تطبيقتخفيض()
    }

    class مديرنظام {
        +إدارةالحجوزات()
        +إدارةالمستخدمين()
        +إدارةالمحتوى()
        +عرضلوحةالتحكم()
    }

    class حجز {
        +String المعرف
        +Date تاريخالوصول
        +Date تاريخالمغادرة
        +String الحالة
        +float المبلغالإجمالي
        +String طريقةالدفع
        +تأكيد()
        +إلغاء()
        +حسابالسعر()
    }

    class غرفة {
        +String المعرف
        +String الاسم
        +String النوع
        +float سعرالليلة
        +String الوصف
        +List الصور
        +int السعة
        +boolean متاحة
        +التحققمنالتوفر(التواريخ)
    }

    class خدمة {
        +String المعرف
        +String الاسم
        +String الفئة
        +float السعر
        +String الوصف
        +هلهيمتاحة()
    }

    class مطعم {
        +String نوعالمطبخ
        +String ساعاتالعمل
        +List القائمة
        +عرضالقائمة()
    }

    class سبا {
        +List العلاجات
        +String ساعاتالعمل
        +حجزعلاج(العلاج, التاريخ)
    }

    class قاعةأفراح {
        +int الطاقةالاستيعابية
        +List التجهيزات
        +التحققمنالتوفر(التاريخ)
    }

    class طبق {
        +String الاسم
        +float السعر
        +String الوصف
        +String الفئة
    }

    class AtlasChat {
        +String مفتاحAPI
        +List سجلالمحادثة
        +String اللغة
        +إرسالرسالة(النص)
        +استقبالرد()
        +تغييراللغة(lang)
    }

    class رسالة {
        +String المحتوى
        +String الدور
        +Date الطابعالزمني
    }

    class نظامالترجمة {
        +String اللغةالنشطة
        +Map قاموسالترجمة
        +تغييراللغة(lang)
        +ترجمة(مفتاح)
        +تطبيقالترجمات()
    }

    مستخدم <|-- عميل
    مستخدم <|-- مديرنظام
    عميل "1" --> "0..*" حجز : يجري
    حجز "1" --> "1" غرفة : يخص
    حجز "1" --> "0..1" خدمة : يشمل
    خدمة <|-- مطعم
    خدمة <|-- سبا
    خدمة <|-- قاعةأفراح
    مطعم "1" --> "0..*" طبق : يقدم
    عميل "1" --> "0..1" AtlasChat : يستخدم
    AtlasChat "1" --> "0..*" رسالة : يحتوي
    نظامالترجمة ..> مستخدم : يدعم
```

**الوصف:** يُجسّد هذا المخطط الهيكل الثابت للنظام بـ 12 فئة. يتضح التسلسل الهرمي (مستخدم ← عميل / مدير) والتركيب (AtlasChat يحتوي رسائل) والتخصص (الخدمة تتفرع إلى مطعم، سبا، قاعة أفراح).

---

## 3. مخطط التسلسل — حجز غرفة

```mermaid
sequenceDiagram
    actor ع as العميل
    participant واجهة as الواجهة البرمجية
    participant ج as script.js
    participant ق as Supabase
    participant ب as إشعار بريدي

    ع->>واجهة: يفتح صفحة الإقامات
    واجهة->>ج: تحميل الصفحة + فحص الجلسة
    ج->>ق: getSession()
    ق-->>ج: بيانات العميل المسجل
    ج-->>واجهة: عرض قائمة الغرف المتاحة

    ع->>واجهة: يضغط تفاصيل على غرفة
    واجهة->>ج: openRoomModal(roomId)
    ج-->>واجهة: عرض نافذة التفاصيل (صور، سعر، وصف)

    ع->>واجهة: يضغط زر الحجز
    واجهة->>ج: openBookingModal(roomId)
    ج-->>واجهة: عرض نموذج الحجز

    ع->>واجهة: يملأ تاريخ الوصول والمغادرة وعدد الأشخاص
    ع->>واجهة: يضغط تأكيد الحجز
    واجهة->>ج: submitReservation(data)
    ج->>ج: validateDates() + calculatePrice()

    alt البيانات صحيحة
        ج->>ق: INSERT INTO reservations
        ق-->>ج: المعرف + الحالة مؤكد
        ج->>ق: UPDATE نقاط_الولاء
        ق-->>ج: تم التحديث
        ج-->>واجهة: رسالة نجاح
        ج->>ب: إرسال بريد التأكيد
    else البيانات غير صحيحة
        ج-->>واجهة: رسالة خطأ
    end
```

**الوصف:** يصف هذا المخطط تدفق حجز الغرفة خطوةً بخطوة، من فتح الصفحة حتى التأكيد في قاعدة البيانات. يُبرز التفاعل بين الواجهة ومنطق JavaScript وخلفية Supabase، مع إدارة الأخطاء وتحديث نقاط الولاء.

---

## 4. مخطط التسلسل — مساعد Atlas Chat الذكي

```mermaid
sequenceDiagram
    actor ع as العميل
    participant ص as chat.html
    participant ج as AtlasChat JS
    participant د as DeepSeek API
    participant ق as Supabase

    ع->>ص: يفتح صفحة Atlas Chat
    ص->>ج: initAtlasChat()
    ج->>ق: getSession()
    ق-->>ج: بيانات المستخدم + اللغة المفضلة
    ج-->>ص: رسالة ترحيب بلغة العميل

    loop محادثة تفاعلية
        ع->>ص: يكتب رسالة ويضغط إرسال
        ص->>ج: sendMessage(النص)
        ج->>ج: بناء نص السياق متعدد اللغات
        ج->>د: POST /v1/chat/completions
        د-->>ج: رد النموذج
        ج-->>ص: عرض الرد مع تأثير الكتابة التدريجية
    end

    ع->>ص: يضغط احجز الآن
    ص->>ج: redirectToBooking()
    ج-->>ص: إعادة توجيه إلى صفحة الإقامات
```

**الوصف:** يوضح هذا المخطط آلية عمل المساعد الذكي Atlas Chat. يتفاعل العميل مع صفحة المحادثة التي ترسل الأسئلة إلى واجهة DeepSeek البرمجية عبر طلبات HTTP آمنة، وتُبنى نصائح السياق ديناميكيًا حسب لغة المستخدم المحفوظة في Supabase.

---

## 5. مخطط النشاط

```mermaid
flowchart TD
    بداية(["بداية"]) --> أ["فتح بوابة Golden Atlas"]
    أ --> ب{"العميل مسجل الدخول؟"}

    ب -- لا --> ج["عرض صفحة تسجيل الدخول"]
    ج --> د{"طريقة الدخول"}
    د -- "بريد إلكتروني + كلمة مرور" --> هـ["إدخال البيانات"]
    د -- "Google OAuth" --> و["المصادقة عبر Google"]
    هـ --> ز["Supabase Auth"]
    و --> ز
    ز --> ح{"البيانات صحيحة؟"}
    ح -- لا --> ط["عرض رسالة خطأ"] --> ج
    ح -- نعم --> ي["توجيه إلى لوحة العميل"]

    ب -- نعم --> ي

    ي --> ك["عرض لوحة التحكم"]
    ك --> ل{"اختيار خدمة"}

    ل --> م["تصفح الإقامات"]
    ل --> ن["تصفح المطاعم"]
    ل --> س["تصفح السبا / المسبح"]
    ل --> ع["فتح Atlas Chat"]

    م --> ف["اختيار غرفة"]
    ف --> ص["عرض التفاصيل"]
    ص --> ق["الضغط على زر الحجز"]
    ق --> ر["ملء نموذج الحجز"]
    ر --> ش{"التحقق من البيانات"}

    ش -- غير صحيحة --> ت["رسالة خطأ"] --> ر
    ش -- صحيحة --> ث["حساب المبلغ الإجمالي"]
    ث --> خ{"استخدام نقاط الولاء؟"}
    خ -- نعم --> ذ["تطبيق التخفيض"]
    خ -- لا --> ض["المبلغ بدون خصم"]
    ذ --> غ["تسجيل الحجز في Supabase"]
    ض --> غ
    غ --> ظ["إضافة نقاط ولاء جديدة"]
    ظ --> ع2["إرسال بريد التأكيد"]
    ع2 --> نهاية(["نهاية"])

    style بداية fill:#e0c97f,color:#1a1a2e,stroke:#e0c97f,stroke-width:2px
    style نهاية fill:#e0c97f,color:#1a1a2e,stroke:#e0c97f,stroke-width:2px
    style ز fill:#1a3c2e,color:#4ade80,stroke:#4ade80
    style غ fill:#1a3c2e,color:#4ade80,stroke:#4ade80
    style ط fill:#3c1a1a,color:#f87171,stroke:#f87171
    style ت fill:#3c1a1a,color:#f87171,stroke:#f87171
```

**الوصف:** يغطي هذا المخطط المسار الكامل للمستخدم من فتح البوابة حتى التأكيد النهائي للحجز. يمثل نقاط القرار الحرجة (المصادقة، التحقق من البيانات، استخدام نقاط الولاء) والمسارات البديلة عند الأخطاء.

---

## 6. مخطط النشر

```mermaid
flowchart TD
    subgraph جانبالعميل["💻 جانب العميل"]
        متصفح["🌐 متصفح الويب\nChrome / Firefox / Safari\n─────────────────\nHTML5 + CSS3 + Vanilla JS\nمحرك الترجمة (عربي/فرنسي/إنجليزي/إسباني)"]
    end

    subgraph نتلفاي["☁️ Netlify — استضافة ثابتة"]
        ملفات["📁 الملفات الثابتة\n─────────────────\nindex.html / hebergements.html\nchat.html / login.html\nstyle.css / script.js\nصور ومحتوى"]
        شبكة["🌍 شبكة CDN Netlify\nHTTPS / TLS\nتوزيع جغرافي عالمي"]
    end

    subgraph سوبابيس["🗄️ Supabase — خدمات الخلفية"]
        مصادقة["🔐 خدمة المصادقة\nبريد إلكتروني + Google OAuth 2.0"]
        قاعدة["🐘 قاعدة بيانات PostgreSQL\n─────────────────\nجدول المستخدمون\nجدول الحجوزات\nجدول الغرف\nجدول الخدمات"]
        واجهة["🔌 واجهة REST API\nأمان على مستوى الصف"]
    end

    subgraph ديبسيك["🤖 DeepSeek — واجهة الذكاء الاصطناعي"]
        نموذج["🧠 نموذج deepseek-chat\nنموذج لغوي متعدد اللغات\n─────────────────\nنص سياق الفندق\nاستجابة JSON"]
    end

    subgraph خدماتجوجل["🔑 خدمات Google"]
        أوث["Google OAuth 2.0\nمزود الهوية"]
        خطوط["Google Fonts\nInter / Playfair Display"]
    end

    متصفح -->|"HTTPS GET"| شبكة
    شبكة --> ملفات
    ملفات --> متصفح
    متصفح -->|"Supabase JS Client HTTPS"| واجهة
    واجهة --> مصادقة
    واجهة --> قاعدة
    مصادقة -->|"تدفق OAuth"| أوث
    أوث -->|"رمز الهوية"| مصادقة
    متصفح -->|"fetch POST Bearer Token"| نموذج
    متصفح -->|"استيراد CSS"| خطوط

    style جانبالعميل fill:#1a1a2e,color:#e0c97f,stroke:#e0c97f,stroke-width:2px
    style نتلفاي fill:#00435a,color:#fff,stroke:#38bdf8,stroke-width:2px
    style سوبابيس fill:#1a3c2e,color:#4ade80,stroke:#4ade80,stroke-width:2px
    style ديبسيك fill:#2d1b69,color:#a78bfa,stroke:#7c3aed,stroke-width:2px
    style خدماتجوجل fill:#3c2400,color:#fbbf24,stroke:#f59e0b,stroke-width:2px
```

**الوصف:** يمثل هذا المخطط البنية التحتية الكاملة لنشر بوابة Golden Atlas. الملفات الثابتة مستضافة على Netlify بشبكة CDN عالمية. الخلفية تعتمد بالكامل على Supabase (قاعدة البيانات + المصادقة + واجهة REST). المساعد الذكي يعمل عبر DeepSeek API والمصادقة الاجتماعية تتم عبر Google OAuth 2.0.

---

## جدول الملخص

| # | المخطط | الهدف | العناصر الرئيسية |
|---|--------|--------|-----------------|
| 1 | **مخطط حالات الاستخدام** | أدوار الأطراف وحالات الاستخدام | 3 أطراف، 13 حالة، شمول/امتداد |
| 2 | **مخطط الفئات** | هيكل البيانات والعلاقات | 12 فئة، توارث، ارتباطات، اعتماديات |
| 3 | **مخطط التسلسل — الحجز** | تدفق الحجز خطوة بخطوة | عميل ← واجهة ← script.js ← Supabase |
| 4 | **مخطط التسلسل — Atlas Chat** | تدفق المحادثة مع الذكاء الاصطناعي | عميل ← DeepSeek API ← رد متعدد اللغات |
| 5 | **مخطط النشاط** | المنطق التجاري ونقاط القرار | تسجيل دخول ← تصفح ← حجز ← تأكيد |
| 6 | **مخطط النشر** | البنية التحتية والاستضافة | Netlify + Supabase + DeepSeek + Google |

---

> **ملاحظة:** جميع المخططات مبنية على التقنيات الفعلية للمشروع:
> - **الواجهة الأمامية:** HTML5, CSS3, Vanilla JavaScript
> - **الاستضافة:** Netlify CDN
> - **الخلفية:** Supabase (PostgreSQL + Auth)
> - **الذكاء الاصطناعي:** DeepSeek API
> - **المصادقة:** Google OAuth 2.0
